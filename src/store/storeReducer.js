const initialState = {
    cartItems: []
}

export default function storeReducer(state = initialState, action) {
    //console.log(action)
    //console.log(state)

    switch (action.type) {
        default: return state
    }
}
